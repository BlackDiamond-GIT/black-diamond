"""Admin-only Ukrainian locale — does not affect the public site."""

ADMIN_LOCALE = 'uk'

# Prefixes where Ukrainian is forced (public /cs/booking/ etc. are excluded).
ADMIN_PATH_PREFIXES = ('/admin/',)
